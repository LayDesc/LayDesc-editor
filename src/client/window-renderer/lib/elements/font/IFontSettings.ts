import {UNIT} from "../../geometry/generic/UNIT";

export interface IFontSettings {
    fontFamily?: string,
    fontSize?: number,
    lineHeight?: number,
    unit?: UNIT,
    letterSpacing?: number,
}